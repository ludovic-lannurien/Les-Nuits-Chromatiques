// === action types
export const HANDLE_SHOWLINKS = 'HANDLE_SHOWLINKS';
export const HANDLE_HIDELINKS = 'HANDLE_HIDELINKS';

export const handleShowLinks = () => ({
  type: HANDLE_SHOWLINKS,
});

export const handleHideLinks = () => ({
  type: HANDLE_SHOWLINKS,
});
